CREATE TABLE mapsTable(
	`id` INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
	`Date` DATE,
	`Time` TIMESTAMP,
	`Weekday` VARCHAR(50),
	`toHome` VARCHAR(70),
	`toWork` VARCHAR(70)
);
	
-- mysql password is raspberry
